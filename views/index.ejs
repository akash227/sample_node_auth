<!DOCTYPE html>
<html>
<head>
	<title>Passport</title>
	
</head>
<body>
	<div class="container">
	<% if (!isAuthenticated) { %>
		<a href="/login"> Log in here </a>
	<% } else { %>
		Hello, <%= user.name %>!
	<% } %>
	</div>
</body>

 </html>